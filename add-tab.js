(() => {
  const DB_NAME='lukWardrobeDB',DB_VERSION=1,STORE_CLOTHES='customClothes',STORE_PROFILE='profilePhotos';
  const typeMap={outer:['Верхняя одежда','outer'],layer:['Рубашки','layer'],top:['Футболки / худи','top'],bottom:['Брюки / шорты','bottom']};
  let customRecords=[];

  const style=document.createElement('style');
  style.textContent=`
  .add-view{padding-top:28px}.add-hero{background:linear-gradient(135deg,#111 0 56%,#cfff25 56%);color:#fff;border-radius:32px;padding:28px;min-height:220px;display:flex;align-items:flex-end;position:relative;overflow:hidden;box-shadow:var(--shadow)}.add-hero:after{content:"+";position:absolute;right:7%;top:5%;font-size:160px;line-height:1;color:#111;font-weight:1000;transform:rotate(8deg)}.add-hero h2{font-size:clamp(34px,7vw,72px);letter-spacing:-.065em;line-height:.9;margin:0 0 10px;max-width:620px}.add-hero p{margin:0;color:#c9c9c9;max-width:540px;line-height:1.45}.add-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:18px}.upload-panel{background:#fff;border:1px solid var(--line);border-radius:28px;padding:20px;box-shadow:var(--shadow)}.upload-panel h3{font-size:25px;letter-spacing:-.04em;margin:0 0 6px}.upload-panel>p{color:var(--muted);font-size:13px;line-height:1.45;margin:0 0 16px}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.field{display:grid;gap:6px}.field.full{grid-column:1/-1}.field label{font-size:11px;font-weight:900;text-transform:uppercase;letter-spacing:.08em;color:#68635d}.field input,.field select,.field textarea{width:100%;border:1px solid var(--line);background:#faf8f4;border-radius:14px;padding:12px;outline:none}.field textarea{min-height:80px;resize:vertical}.drop{border:2px dashed #cfc8bd;border-radius:20px;background:#f8f5ef;padding:16px;min-height:154px;display:grid;place-items:center;text-align:center;position:relative;overflow:hidden}.drop input{position:absolute;inset:0;opacity:0;cursor:pointer}.drop b{display:block}.drop small{display:block;color:var(--muted);margin-top:5px}.drop-preview{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}.upload-actions{display:flex;gap:10px;margin-top:14px}.upload-actions button{flex:1;border:0;border-radius:16px;padding:13px;font-weight:950}.save-clothing,.save-profile{background:#111;color:#fff}.secondary-action{background:var(--lime);color:#111}.photo-instruction{display:grid;gap:9px;margin:12px 0}.instruction-row{display:grid;grid-template-columns:38px 1fr;gap:10px;align-items:start;background:#f6f2ec;border-radius:15px;padding:10px}.instruction-row span{width:38px;height:38px;border-radius:12px;background:var(--lime);display:grid;place-items:center;font-weight:1000}.instruction-row b{font-size:12px}.instruction-row small{display:block;color:var(--muted);font-size:10px;line-height:1.35;margin-top:2px}.profile-slots{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.profile-slot{min-height:170px}.saved-uploads{margin-top:18px;background:#fff;border:1px solid var(--line);border-radius:28px;padding:20px}.saved-uploads h3{margin:0 0 12px;font-size:24px}.custom-list{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.custom-card{background:#f4f0e9;border-radius:18px;overflow:hidden}.custom-card img{width:100%;aspect-ratio:1;object-fit:cover}.custom-card-body{padding:10px}.custom-card b{display:block;font-size:12px}.custom-card small{display:block;color:var(--muted);font-size:9px;margin-top:3px}.custom-card button{width:100%;border:0;border-top:1px solid #ddd4c8;background:#fff;padding:9px;font-weight:850}.privacy-note{margin-top:12px;font-size:11px;color:var(--muted);line-height:1.4}.icon-note{margin-top:10px;padding:10px 12px;border-radius:14px;background:#eef7d4;border:1px solid #c5df65;font-size:11px;line-height:1.4}.upload-toast{position:fixed;left:50%;bottom:calc(24px + env(safe-area-inset-bottom));transform:translate(-50%,20px);background:#111;color:#fff;border:2px solid var(--lime);border-radius:999px;padding:13px 17px;font-weight:900;opacity:0;pointer-events:none;transition:.25s;z-index:900;white-space:nowrap}.upload-toast.show{opacity:1;transform:translate(-50%,0)}
  @media(max-width:760px){.add-view{padding-top:16px}.add-hero{border-radius:24px;padding:20px;min-height:190px;background:linear-gradient(145deg,#111 0 68%,#cfff25 68%)}.add-hero:after{font-size:110px;right:1%;top:20%}.add-grid{grid-template-columns:1fr}.upload-panel{border-radius:22px;padding:15px}.form-grid{grid-template-columns:1fr}.field.full{grid-column:auto}.custom-list{grid-template-columns:repeat(2,minmax(0,1fr))}.profile-slots{grid-template-columns:1fr 1fr}.nav{max-width:calc(100vw - 94px)}.nav button{white-space:nowrap}}
  `;
  document.head.appendChild(style);

  function openDB(){return new Promise((resolve,reject)=>{const req=indexedDB.open(DB_NAME,DB_VERSION);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(STORE_CLOTHES))db.createObjectStore(STORE_CLOTHES,{keyPath:'id'});if(!db.objectStoreNames.contains(STORE_PROFILE))db.createObjectStore(STORE_PROFILE,{keyPath:'key'})};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
  async function put(store,value){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).put(value);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}
  async function getAll(store){const db=await openDB();return new Promise((resolve,reject)=>{const req=db.transaction(store).objectStore(store).getAll();req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
  async function del(store,key){const db=await openDB();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).delete(key);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}
  const fileToData=file=>new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)});
  const loadImage=src=>new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=reject;img.src=src});

  function toast(text){let t=document.querySelector('#uploadToast');if(!t){t=document.createElement('div');t.id='uploadToast';t.className='upload-toast';document.body.appendChild(t)}t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400)}
  function bindPreview(input,box){input.addEventListener('change',()=>{const f=input.files?.[0];box.querySelector('.drop-preview')?.remove();if(!f)return;const img=document.createElement('img');img.className='drop-preview';img.src=URL.createObjectURL(f);box.appendChild(img)})}

  async function makeCartoonIcon(dataUrl){
    const img=await loadImage(dataUrl),size=640,canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;
    const ctx=canvas.getContext('2d',{willReadFrequently:true});
    ctx.fillStyle='#faf8f3';ctx.fillRect(0,0,size,size);
    const scale=Math.min((size-86)/img.width,(size-86)/img.height),w=img.width*scale,h=img.height*scale,x=(size-w)/2,y=(size-h)/2;
    const work=document.createElement('canvas');work.width=size;work.height=size;const wctx=work.getContext('2d',{willReadFrequently:true});wctx.drawImage(img,x,y,w,h);
    const image=wctx.getImageData(0,0,size,size),d=image.data;
    const samples=[];for(let i=0;i<size;i+=16){for(const [sx,sy] of [[i,4],[i,size-5],[4,i],[size-5,i]]){const p=(sy*size+sx)*4;samples.push([d[p],d[p+1],d[p+2]])}}
    samples.sort((a,b)=>(a[0]+a[1]+a[2])-(b[0]+b[1]+b[2]));const bg=samples[Math.floor(samples.length*.55)]||[245,245,245];
    const mask=new Uint8ClampedArray(size*size);
    for(let py=0;py<size;py++)for(let px=0;px<size;px++){const p=(py*size+px)*4,dr=d[p]-bg[0],dg=d[p+1]-bg[1],db=d[p+2]-bg[2],dist=Math.sqrt(dr*dr+dg*dg+db*db),inside=px>28&&px<size-28&&py>28&&py<size-28;mask[py*size]=inside&&dist>34?255:0}
    const copy=new Uint8ClampedArray(mask);for(let py=1;py<size-1;py++)for(let px=1;px<size-1;px++){let n=0;for(let yy=-1;yy<=1;yy++)for(let xx=-1;xx<=1;xx++)n+=copy[(py+yy)*size+px+xx]>0?1:0;mask[py*size+px]=n>=4?255:0}
    const out=ctx.createImageData(size,size),o=out.data;
    for(let py=0;py<size;py++)for(let px=0;px<size;px++){const p=(py*size+px)*4,m=mask[py*size+px];if(!m)continue;let r=d[p],g=d[p+1],b=d[p+2];r=Math.round(r/28)*28;g=Math.round(g/28)*28;b=Math.round(b/28)*28;let edge=false;for(let yy=-2;yy<=2&&!edge;yy++)for(let xx=-2;xx<=2;xx++){const ni=(py+yy)*size+px+xx;if(py+yy<0||py+yy>=size||px+xx<0||px+xx>=size||!mask[ni]){edge=true;break}}if(edge){r=30;g=25;b=22}o[p]=r;o[p+1]=g;o[p+2]=b;o[p+3]=255}
    const temp=document.createElement('canvas');temp.width=size;temp.height=size;temp.getContext('2d').putImageData(out,0,0);
    ctx.save();ctx.shadowColor='rgba(35,25,18,.24)';ctx.shadowBlur=24;ctx.shadowOffsetY=14;ctx.drawImage(temp,0,0);ctx.restore();ctx.drawImage(temp,0,0);
    return canvas.toDataURL('image/webp',.9);
  }

  function inject(){
    if(typeof items==='undefined'||typeof renderCatalog!=='function'||typeof go!=='function')return setTimeout(inject,120);
    if(document.querySelector('#add'))return;
    const nav=document.querySelector('.nav'),btn=document.createElement('button');btn.dataset.view='add';btn.textContent='Добавить';btn.onclick=()=>go('add');nav.appendChild(btn);
    const section=document.createElement('section');section.id='add';section.className='view add-view';section.innerHTML=`
      <div class="add-hero"><div><h2>Добавь новое.</h2><p>Загружай вещи и свои референсные фотографии. Они хранятся только на этом устройстве.</p></div></div>
      <div class="add-grid"><article class="upload-panel"><h3>Новый предмет одежды</h3><p>Загрузи оригинальное фото — приложение автоматически создаст красивую мультяшную иконку для каталога.</p><div class="form-grid">
      <div class="field"><label>Название</label><input id="newName" placeholder="Например, зелёная куртка"></div><div class="field"><label>Категория</label><select id="newSlot"><option value="outer">Верхняя одежда</option><option value="layer">Рубашка / дополнительный слой</option><option value="top">Футболка, худи или лонгслив</option><option value="bottom">Брюки или шорты</option></select></div><div class="field"><label>Цвет</label><input id="newColor" placeholder="Оливковый"></div><div class="field"><label>Стиль</label><input id="newStyle" placeholder="Casual / streetwear"></div>
      <div class="field full"><label>Фото вещи отдельно — обязательно</label><div class="drop" id="flatDrop"><input id="newFlat" type="file" accept="image/*"><div><b>Выбрать оригинальное фото</b><small>Вся вещь целиком, ровный свет, контрастный фон</small></div></div></div>
      <div class="field full"><label>Фото вещи на тебе — желательно</label><div class="drop" id="wornDrop"><input id="newWorn" type="file" accept="image/*"><div><b>Добавить фото посадки</b><small>Используется только для реалистичной примерки</small></div></div></div><div class="field full"><label>Заметка</label><textarea id="newNote" placeholder="Бренд, материал, сезон, особенности посадки"></textarea></div></div>
      <div class="icon-note">В каталоге будет показана мультяшная иконка. Оригинальная фотография сохранится отдельно и будет использоваться при генерации примерки.</div><div class="upload-actions"><button class="save-clothing" id="saveClothing">Создать иконку и добавить</button><button class="secondary-action" id="resetClothing">Очистить</button></div></article>
      <article class="upload-panel"><h3>Мои фотографии</h3><p>Фотографии нужны как строгие референсы внешности, фигуры и татуировок.</p><div class="photo-instruction"><div class="instruction-row"><span>1</span><div><b>Полный рост спереди</b><small>Ровная стойка, всё тело в кадре.</small></div></div><div class="instruction-row"><span>2</span><div><b>Полный рост сбоку</b><small>Нейтральная поза без втягивания живота.</small></div></div><div class="instruction-row"><span>3</span><div><b>Лицо крупным планом</b><small>Без фильтров и сильных теней.</small></div></div><div class="instruction-row"><span>4</span><div><b>Татуировки и детали</b><small>Чёткие отдельные кадры.</small></div></div></div><div class="profile-slots">
      <div class="field"><label>Спереди</label><div class="drop profile-slot" id="frontDrop"><input id="profileFront" type="file" accept="image/*"><div><b>Полный рост</b></div></div></div><div class="field"><label>Сбоку</label><div class="drop profile-slot" id="sideDrop"><input id="profileSide" type="file" accept="image/*"><div><b>Вид сбоку</b></div></div></div><div class="field"><label>Лицо</label><div class="drop profile-slot" id="faceDrop"><input id="profileFace" type="file" accept="image/*"><div><b>Портрет</b></div></div></div><div class="field"><label>Детали</label><div class="drop profile-slot" id="detailDrop"><input id="profileDetail" type="file" accept="image/*"><div><b>Татуировки</b></div></div></div></div><div class="upload-actions"><button class="save-profile" id="saveProfile">Сохранить мои фото</button></div><div class="privacy-note">Фото сохраняются локально в браузере и не публикуются.</div></article></div><section class="saved-uploads"><h3>Добавленные вещи</h3><div class="custom-list" id="customList"></div></section>`;
    document.querySelector('main').appendChild(section);
    [['#newFlat','#flatDrop'],['#newWorn','#wornDrop'],['#profileFront','#frontDrop'],['#profileSide','#sideDrop'],['#profileFace','#faceDrop'],['#profileDetail','#detailDrop']].forEach(([i,b])=>bindPreview(document.querySelector(i),document.querySelector(b)));
    document.querySelector('#saveClothing').onclick=saveClothing;document.querySelector('#resetClothing').onclick=resetClothing;document.querySelector('#saveProfile').onclick=saveProfile;hydrate();
  }

  async function saveClothing(){
    const button=document.querySelector('#saveClothing'),name=document.querySelector('#newName').value.trim(),slot=document.querySelector('#newSlot').value,color=document.querySelector('#newColor').value.trim()||'Не указан',styleName=document.querySelector('#newStyle').value.trim()||'Custom',flat=document.querySelector('#newFlat').files[0],worn=document.querySelector('#newWorn').files[0],note=document.querySelector('#newNote').value.trim();
    if(!name)return alert('Укажи название вещи');if(!flat)return alert('Добавь фотографию вещи отдельно');
    button.disabled=true;button.textContent='Создаём мультяшную иконку…';
    try{const flatData=await fileToData(flat),iconData=await makeCartoonIcon(flatData),id='C-'+Date.now().toString(36).toUpperCase(),rec={id,name,slot,cat:typeMap[slot][0],color,style:styleName,note,flatData,iconData,wornData:worn?await fileToData(worn):null,createdAt:Date.now()};await put(STORE_CLOTHES,rec);customRecords.push(rec);addToRuntime(rec);resetClothing();renderCustomList();renderCatalog();if(typeof renderBuilder==='function')renderBuilder();toast('✓ Иконка создана, вещь добавлена')}catch(e){console.error(e);alert('Не удалось создать иконку. Попробуй другое фото.')}finally{button.disabled=false;button.textContent='Создать иконку и добавить'}
  }
  function resetClothing(){['#newName','#newColor','#newStyle','#newNote'].forEach(s=>document.querySelector(s).value='');['#newFlat','#newWorn'].forEach(s=>document.querySelector(s).value='');['#flatDrop','#wornDrop'].forEach(s=>document.querySelector(s).querySelector('.drop-preview')?.remove())}
  async function saveProfile(){const slots=[['front','#profileFront'],['side','#profileSide'],['face','#profileFace'],['detail','#profileDetail']];let n=0;for(const [key,sel] of slots){const f=document.querySelector(sel).files[0];if(f){await put(STORE_PROFILE,{key,data:await fileToData(f),updatedAt:Date.now()});n++}}if(!n)return alert('Выбери хотя бы одну фотографию');toast('✓ Референсные фотографии сохранены')}
  function addToRuntime(rec){if(items.some(x=>x.id===rec.id))return;items.push({id:rec.id,name:rec.name,cat:rec.cat,color:rec.color,style:rec.style,slot:rec.slot,flatKey:rec.id+'_custom_flat',wornKey:rec.wornData?rec.id+'_custom_worn':null,custom:true})}
  function wrapPic(){if(window.__addPicWrapped)return;window.__addPicWrapped=true;const prev=pic;pic=function(key){const id=key?.replace('_custom_flat','').replace('_custom_worn',''),rec=customRecords.find(r=>r.id===id);if(rec){const src=key.includes('_custom_worn')&&rec.wornData?rec.wornData:(rec.iconData||rec.flatData);return `<div class="pic" style="background-image:url('${src}');background-size:cover;background-position:center"></div>`}return prev(key)}}
  async function hydrate(){customRecords=await getAll(STORE_CLOTHES);for(const rec of customRecords){if(!rec.iconData&&rec.flatData){try{rec.iconData=await makeCartoonIcon(rec.flatData);await put(STORE_CLOTHES,rec)}catch(e){}}addToRuntime(rec)}wrapPic();renderCustomList();renderCatalog();if(typeof renderBuilder==='function')renderBuilder()}
  function renderCustomList(){const el=document.querySelector('#customList');if(!el)return;el.innerHTML=customRecords.length?customRecords.sort((a,b)=>b.createdAt-a.createdAt).map(r=>`<article class="custom-card"><img src="${r.iconData||r.flatData}" alt="${r.name}"><div class="custom-card-body"><b>${r.name}</b><small>${r.cat} · ${r.color}</small></div><button data-delete-custom="${r.id}">Удалить</button></article>`).join(''):'<div class="empty">Ты пока не добавил новые вещи.</div>';el.querySelectorAll('[data-delete-custom]').forEach(b=>b.onclick=async()=>{if(!confirm('Удалить эту вещь с устройства?'))return;await del(STORE_CLOTHES,b.dataset.deleteCustom);customRecords=customRecords.filter(r=>r.id!==b.dataset.deleteCustom);const i=items.findIndex(x=>x.id===b.dataset.deleteCustom);if(i>=0)items.splice(i,1);renderCustomList();renderCatalog();if(typeof renderBuilder==='function')renderBuilder();toast('Вещь удалена')})}

  window.LUKCustomClothes={async getDataURL(id,mode='flat'){const rec=customRecords.find(r=>r.id===id);if(!rec)throw new Error('Вещь не найдена');return mode==='worn'&&rec.wornData?rec.wornData:rec.flatData},async getIconDataURL(id){const rec=customRecords.find(r=>r.id===id);return rec?.iconData||null}};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject);else inject();
})();
